package operations

// This file was generated by the swagger tool.
// Editing this file might prove futile when you re-run the swagger generate command

import (
	"io"
	"net/http"

	"github.com/go-openapi/errors"
	"github.com/go-openapi/runtime"
	"github.com/go-openapi/runtime/middleware"

	"github.com/hpcloud/cf-usb/lib/genmodel"
)

// NewRegisterDriverEndpointParams creates a new RegisterDriverEndpointParams object
// with the default values initialized.
func NewRegisterDriverEndpointParams() RegisterDriverEndpointParams {
	var ()
	return RegisterDriverEndpointParams{}
}

// RegisterDriverEndpointParams contains all the bound params for the register driver endpoint operation
// typically these are obtained from a http.Request
//
// swagger:parameters registerDriverEndpoint
type RegisterDriverEndpointParams struct {

	// HTTP Request Object
	HTTPRequest *http.Request

	/*An endpoint to be registered with the USB
	  Required: true
	  In: body
	*/
	DriverEndpoint *genmodel.DriverEndpoint
}

// BindRequest both binds and validates a request, it assumes that complex things implement a Validatable(strfmt.Registry) error interface
// for simple values it will use straight method calls
func (o *RegisterDriverEndpointParams) BindRequest(r *http.Request, route *middleware.MatchedRoute) error {
	var res []error
	o.HTTPRequest = r

	if runtime.HasBody(r) {
		defer r.Body.Close()
		var body genmodel.DriverEndpoint
		if err := route.Consumer.Consume(r.Body, &body); err != nil {
			if err == io.EOF {
				res = append(res, errors.Required("driverEndpoint", "body"))
			} else {
				res = append(res, errors.NewParseError("driverEndpoint", "body", "", err))
			}

		} else {
			if err := body.Validate(route.Formats); err != nil {
				res = append(res, err)
			}

			if len(res) == 0 {
				o.DriverEndpoint = &body
			}
		}

	} else {
		res = append(res, errors.Required("driverEndpoint", "body"))
	}

	if len(res) > 0 {
		return errors.CompositeValidationError(res...)
	}
	return nil
}
